(defmacro let-if (if-condition then-bindings else-bindings &body body)
  `(if ,if-condition
     (let* ,then-bindings
       ,@body)
     (let* ,else-bindings
       ,@body)))

(let-if t ((a 5)) ((a 7)) a)

(macroexpand '(let-if t ((a 5)) ((a 7))))
